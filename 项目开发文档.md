# 电商用户行为画像与购买倾向系统开发文档

## 1. 项目目标

构建基于京东 IJCAI 19 数据集的电商用户行为画像与购买倾向预测系统，目标包括：
- 生成用户画像分群，支持用户分类与运营策略
- 预测用户近期购买倾向，辅助新客激活与老客召回
- 提供可解释性分析与模型评估报告

## 2. 当前项目资源

### 2.1 数据文件
- `data/JD_IJCAI 19/user`：用户基础信息
- `data/JD_IJCAI 19/item_info`：商品信息，包含 `price`、类别、品牌等
- `data/JD_IJCAI 19/user_click`：用户点击行为
- `data/JD_IJCAI 19/user_order`：用户订单行为

| 数据集 | 记录数 | 字段数 | 主要字段 |
|--------|--------|--------|----------|
| 用户数据 | 100,000 | 3 | user_id, gender, age_range |
| 商品数据 | 6,827,234 | 11 | item_id, cid1, cid2, cid3, cid1_name, cid2_name, cid3_name, brand_code, price, item_name, seg_name |
| 用户点击数据 | 52,983,323 | 4 | user_id, item_id, datetime, dt |
| 用户订单数据 | 1,458,498 | 5 | user_id, item_id, count, datetime, dt |


### 2.2 现有代码与文档
- `Preprocessing.ipynb`：已完成数据读取、数据清洗、price 缺失处理与 Parquet 转换
- `项目目标.md`：项目目标与技术栈简要说明
- `项目可行性分析报告.md`：原可行性分析已转为开发文档参考内容

## 3. 现状总结

### 3.1 关键发现
- 商品数据包含约 205 万条 `price` 缺失，占整体约 30%
- 已尝试多种模型预测价格，最好结果来自 LightGBM：
  - RMSE 1.1275
  - MAE 0.8397
  - R² 0.5267
- 该结果表明：
  - 仅依靠类别特征预测精确价格存在较大误差
  - 直接将单点预测价格作为精细定价特征风险较高

### 3.2 结论
- `price` 缺失处理应优先采用：
  1. 缺失标记（价格是否缺失）
  2. 以同类别/同品牌商品组内中位数或平均数补全
- LightGBM 预测结果可作为辅助补充，用于判断价格区间或填补较大缺失块，但不作为主特征
- ✅ 已完成数据清洗：无缺失值，添加了 price_missing 标记，价格范围合理

## 4. 开发模块与实施策略

### 4.1 数据预处理
- 读取并检查原始数据格式与缺失情况
- 优化数据类型，减少内存占用
- 清理异常值与重复记录
- 保存清洗后的商品数据为 `item_data_cleaned.csv`
- 将关键数据集转换为 Parquet 格式，提升后续读取性能

### 4.2 商品价格缺失处理
- 统计 `price` 缺失分布
- 计算 `cid1/cid2/brand_code` 维度的价格中位数和平均数
- 生成补全策略：缺失标记 + 组内统计值补全
- 备用策略：LightGBM 预测价格作为区间参考

### 4.3 特征工程
- 用户行为特征：点击次数、浏览类别、最近行为时间、转化漏斗位置
- 订单特征：RFM、复购频率、客单价等
- 商品特征：分类层级、品牌、价格区间、是否缺失价格
- 画像特征：用户性别、年龄段、兴趣类别分布

### 4.4 用户分群与画像
- 采用 K-Means 或类似聚类算法
- 目标分群数可先从 3-5 个试验
- 解释聚类结果：各群特征、核心画像标签

### 4.5 购买倾向预测
- 任务形式：二分类（本周是否下单 / 是否复购）
- 候选模型：逻辑回归、决策树、随机森林、XGBoost/LightGBM
- 重点处理类别不平衡：权重调整、过采样/欠采样、评价指标选取

### 4.6 评估与可视化
- 模型指标：准确率、召回率、F1、AUC
- 可视化：混淆矩阵、特征重要性、价格分布、分群画像
- 生成分析结论与运营建议

## 5. 优先开发计划

### 5.1 当前可立即执行任务
1. ✅ 已完成 `Preprocessing.ipynb` 中的完整数据清洗流程
2. ✅ 已完成 `price` 缺失处理方案实现
3. ✅ 已保存清洗后文件 `data/JD_IJCAI 19/item_data_cleaned.csv`
4. ✅ 已完成 Parquet 转换，生成 `data/parquet/` 数据集
5. ✅ 已完成 `Feature_Engineering.ipynb` 中的用户行为与订单特征构建和用户分群建模与聚类画像分析，生成带 `cluster` 标签的特征文件，保存为 `data/parquet/user_features_raw.parquet` 与 `data/parquet/user_features_scaled.parquet`

### 5.2 中期任务
1. 训练购买倾向预测模型，完成初步评估
2. 进一步优化特征与反欺诈/行为转化类指标
3. 研究标签构建与时间窗口策略，避免未来数据泄露

### 5.3 后期任务
1. 完成模型对比与参数调优
2. 输出项目报告与可视化结果
3. 撰写毕业设计文档与答辩总结

## 6. 后续计划建议

### 6.1 文件结构建议
- `job.ipynb`：主分析流程与实验记录
- `data/JD_IJCAI 19/item_data_cleaned.csv`：清洗后商品数据
- `项目开发文档.md`：开发目标与计划说明

### 6.2 推荐近期行动
- ✅ 已完成 `Preprocessing.ipynb` 中的“数据清洗 + 缺失填充策略 + Parquet 转换”部分
- ✅ 已完成“特征工程 + 分群建模”，目前可进入“购买倾向预测与评估”阶段
- 最后补充“购买倾向预测与评估”模块
