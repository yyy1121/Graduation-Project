# 电商用户行为画像与购买倾向系统开发文档

## 1. 项目目标

使用示例电商点击与订单数据，完成会话重构、去重与异常点击过滤，抽取RFM、最近浏览品类数、加购/收藏比、转化漏斗位置等特征；通过聚类（K-Means）得到用户画像分群，并以“本周是否下单/是否复购”为标签训练逻辑回归/决策树/随机森林；用准确率、召回率、AUC评估，输出混淆矩阵与特征重要性；

技术栈：Python、pandas、NumPy、scikit-learn、Matplotlib；

目标是形成“画像分群+倾向预测”的轻量方案与建议规则，用于新客激活与老客召回。

## 2. 当前项目资源

### 2.1 数据文件

- `data/JD_IJCAI 19/user`：用户基础信息
- `data/JD_IJCAI 19/item_info`：商品信息，包含 `price`、类别、品牌等
- `data/JD_IJCAI 19/user_click`：用户点击行为
- `data/JD_IJCAI 19/user_order`：用户订单行为

| 数据集 | 记录数 | 字段数 | 主要字段 |
|--------|--------|--------|----------|
| 用户数据 | 100,000 | 3 | user_id, gender, age_range |
| 商品数据 | 6,827,234 | 11 | item_id, cid1, cid2, cid3, cid1_name, cid2_name, cid3_name, brand_code, price, item_name, seg_name |
| 用户点击数据 | 52,983,323 | 4 | user_id, item_id, datetime, dt |
| 用户订单数据 | 1,458,498 | 5 | user_id, item_id, count, datetime, dt |

### 2.2 现有代码与文档

- `Preprocessing.ipynb`：已完成数据读取、数据清洗、price 缺失处理与 Parquet 转换
- `Feature_Engineering.ipynb`：已完成特征提取、用户聚类、标准化与保存用户特征结果
- `Purchase_Prediction.ipynb`：已完成滑动窗口特征构建、标签生成、时间顺序划分和模型训练流程
- `项目目标.md`：项目目标与技术栈简要说明
- `项目开发文档.md`：原可行性分析已转为开发文档参考内容

## 3. 已实现的数据处理与特征工程流程

### 3.1 Preprocessing.ipynb

- 读取原始 CSV 数据：`user`、`item_info`、`user_click`、`user_order`。
- 检查数据完整性：查看字段、缺失值、异常值分布。
- `item_info` 数据清洗：
  - `seg_name` 缺失使用 `item_name` 前 10 个字符填充。
  - `brand_code` 与类目 `cid1/cid2/cid3` 缺失填充为 `unknown`。
  - `price` 缺失使用分级中位数填充：优先 `cid3+brand_code`，其次 `cid1+brand_code`，最后全局中位数。
  - `price` 异常值裁剪：使用 0.1% 和 99.9% 分位数做上下限截断。
- 保存清洗后商品表为 `data/JD_IJCAI 19/item_data_cleaned.csv`。
- 将 CSV 数据转换为 Parquet，生成 `data/parquet/user.parquet`、`item_data_cleaned.parquet`、`user_click.parquet`、`user_order.parquet`。

### 3.2 Feature_Engineering.ipynb

- 读取 Parquet 数据并统一类型：`user_id`、`item_id` 转为字符串；`datetime` 转为 datetime 类型。
- 过滤异常点击用户：按用户点击量 99.9% 分位数筛除异常高频点击，减少爬虫/刷单干扰。
- 订单特征构建：
  - 购买总件数、订单次数、独立购买商品数、总消费金额、平均订单金额、平均商品价格、重复购买比例、订单跨度。
  - 计算 RFM 特征：最近购买天数 `R_days`、首次/最后一次购买距今天数、平均订单间隔等。
- 点击特征构建：
  - 总点击数、独立点击商品数、不同类目/品牌点击数、点击跨度、日均点击、最后点击距今天数等。
- 特征合并与缺失填充：
  - 合并用户基础特征与订单、点击特征。
  - 缺失值填充为 0 或最大天数。
  - 生成复合特征：`click_to_buy_ratio`、`order_click_ratio`、活跃天数、`has_order`、`has_click`。
- 基础特征编码：
  - 性别编码为 0/1，异常值填充为 -1。
  - 年龄区间使用 `OrdinalEncoder` 编码。
- 对偏态特征做 `log1p` 变换，便于建模。
- 标准化连续特征。
- 用户分群：使用 KMeans 聚类 4 类，生成 `cluster` 标签。
- 保存结果：`data/parquet/user_features_raw_with_cluster.parquet`、`data/parquet/user_features_scaled_with_cluster.parquet`、`data/parquet/user_order_cleaned.parquet`。

### 3.3 Purchase_Prediction.ipynb

- 读取已预处理数据：`user_features_raw_with_cluster.parquet`、`user_order_cleaned.parquet`、`user_click.parquet`、`item_data_cleaned.csv`。
- 定义时间窗口：预测窗口取最后 7 天，观察窗口取预测窗口前 30 天。
- 观察期订单特征：订单次数、购买总件数、去重商品数、活跃天数、订单跨度、平均每单商品数、平均间隔、距预测窗口起始天数等。
- 观察期点击特征：点击次数、去重点击商品数、类目/品牌去重数、点击活跃天数、点击跨度、日均点击、距预测窗口起始最后点击天数等。
- 特征合并：
  - 合并用户基础特征、聚类标签与滑动窗口订单/点击特征。
  - 填充缺失值并构造转化特征：`click_to_buy_ratio`、`has_conversion`。
- 标签生成：预测窗口内是否下单作为二分类目标。
- 训练/测试划分：按最后活跃时间排序，保证时间顺序划分训练集与测试集。
- 模型训练与评估：训练 Logistic Regression、Decision Tree、Random Forest、LightGBM 等模型，并进行阈值调优与 ROC/PR 评估。

## 4. 当前结果概览

- `user_features_raw_with_cluster.parquet` 结果数据规模：100,000 行，41 列。
- `user_order_cleaned.parquet` 结果数据规模：1,458,498 行。
- 点击数据规模：52,983,323 行。
- 预测标签正样本比例约 13%，存在明显类别不平衡。
- 训练/测试集划分：80,000 行训练、20,000 行测试。
- 聚类结果：4 个用户群组，涵盖核心高价值用户、中坚用户、流失用户和低价值/新用户。

## 5. 主要问题与风险点

- 原始数据质量不稳定，`datetime` 格式差异和 `price` 缺失需要重点排查。
- 点击行为噪声大，高频点击用户可能影响模型稳定性。
- 目标标签不平衡，正样本占比低，需加强不平衡处理。
- 目前只使用固定窗口特征，时序特征和多周期趋势尚未充分建模。
- 处理流程依赖 Notebook，缺乏脚本化与可复现的流水线。
- 特征类型偏简单，高阶行为特征、品类转化特征、用户路径特征仍可扩展。

## 6. 改进方案与下一步计划

1. 统一数据处理管道
   - 把当前 Notebook 流程拆分为可复用脚本或模块。
   - 增加数据校验与血缘说明，确保每次运行可复现。
2. 强化时序特征
   - 引入多个观察窗口（7 天、14 天、30 天、90 天）。
   - 增加时间衰减特征、趋势特征、最近行为变化率。
3. 丰富特征体系
   - 添加类目/品牌转化率、点击-购买路径特征、热销品类偏好、价位段偏好。
   - 考虑用户生命周期阶段和活跃度分层。
4. 提升模型验证
   - 使用时序交叉验证或留出最近时间窗口验证。
   - 推广阈值优化、样本加权、过采样/欠采样策略。
5. 运营洞察输出
   - 结合聚类标签与预测结果，形成“高价值用户激活”“流失用户召回”策略。
   - 输出可解释性报告，支持运营决策。
