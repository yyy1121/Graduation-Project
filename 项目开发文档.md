# 电商用户行为画像与购买倾向系统开发文档

## 1. 项目目标

基于京东 IJCAI 19 电商点击与订单数据，构建"用户画像分群 + 购买倾向预测"系统。通过 RFM 特征提取、K-Means 聚类实现用户分群画像，并以"本周是否下单"为标签训练梯度提升模型（LightGBM / XGBoost / CatBoost），辅以多时间窗口特征、类目/品牌转化特征、行为路径特征和价格敏感度特征，通过 Optuna 超参数优化和 SHAP 可解释性分析，输出可量化的预测结果与运营建议。

**技术栈**：Python、pandas、NumPy、scikit-learn、LightGBM、XGBoost、CatBoost、Optuna、SHAP、imbalanced-learn、Matplotlib、Seaborn、**FastAPI、Plotly.js、Streamlit**

**最终目标**：形成"画像分群 + 倾向预测"的轻量方案与建议规则，用于新客激活与老客召回。

---

## 2. 项目资源

### 2.1 数据文件

| 数据集 | 记录数 | 字段数 | 主要字段 |
|--------|--------|--------|----------|
| 用户数据 | 100,000 | 3 | user_id, gender, age_range |
| 商品数据 | 6,827,234 | 11 | item_id, cid1, cid2, cid3, cid1_name, cid2_name, cid3_name, brand_code, price, item_name, seg_name |
| 用户点击数据 | 52,983,323 | 4 | user_id, item_id, datetime, dt |
| 用户订单数据 | 1,458,498 | 5 | user_id, item_id, count, datetime, dt |

**中间产物（Parquet）**：

| 文件 | 说明 |
|------|------|
| `data/parquet/user.parquet` | 用户基础信息 |
| `data/parquet/item_data_cleaned.parquet` | 清洗后商品表（price 缺失填充、异常值裁剪） |
| `data/parquet/user_click.parquet` | 用户点击数据 |
| `data/parquet/user_order.parquet` / `user_order_cleaned.parquet` | 用户订单数据 |
| `data/parquet/user_features_raw_with_cluster.parquet` | 100,000 用户 × 41 列，含 RFM + 点击统计 + KMeans 聚类标签 |
| `data/parquet/user_features_scaled_with_cluster.parquet` | 标准化版本 |

**最终模型产物**：

| 文件 | 说明 |
|------|------|
| `data/models/best_purchase_model.pkl` | 最优预测模型（XGBoost） |
| `data/models/scaler.pkl` | StandardScaler 实例 |
| `data/models/feature_names.pkl` | 最终 45 维特征名列表 |

### 2.2 代码文件

| 文件 | 职责 | 状态 |
|------|------|------|
| `Preprocessing.ipynb` | 原始数据读取、price 缺失填充、异常值裁剪、CSV→Parquet 转换 | **前置依赖** |
| `Feature_Engineering.ipynb` | RFM 特征提取、点击行为特征、K-Means 聚类（4 类）、标准化、保存特征表 | **前置依赖** |
| `improved_modeling.py` | **主流水线**：时间窗口划分、多窗口特征构建、类目/品牌/路径/价格特征工程、特征选择、不平衡处理、Optuna 超参数优化（LightGBM / XGBoost / CatBoost）、SHAP 分析、模型保存 | **当前使用** |

**可视化系统**：

| 文件 | 职责 | 状态 |
|------|------|------|
| `backend_api.py` | FastAPI 后端服务，25 个 API 端点，含 NaN 安全清洗、静态文件服务 | **当前使用** |
| `static/dashboard.html` | **主要仪表盘** — 独立 SPA，"Warm Gallery" 浅色主题，Plotly.js 渲染，5 个模块 | **当前使用** |
| `dashboard.py` | Streamlit 备用仪表盘（深色主题，功能完整） | 备用 |
| `prepare_dashboard_data.py` | 离线预计算：重建 119 维特征管线 + 全量 100K 用户预测 + 缓存 JSON | **按需运行** |
| `requirements_viz.txt` | 可视化系统依赖清单 | 参考 |

> `Purchase_Prediction.ipynb` 为旧版建模流程（固定 30 天窗口 + 简单模型），已被 `improved_modeling.py` 取代。

---

## 3. 数据处理流水线

### 3.1 前置步骤：Preprocessing.ipynb

- 读取原始 CSV：`user`、`item_info`、`user_click`、`user_order`
- **item_info 清洗**：
  - `seg_name` 缺失 → 使用 `item_name` 前 10 个字符填充
  - `brand_code` / `cid1` / `cid2` / `cid3` 缺失 → 填充为 `unknown`
  - `price` 缺失 → 分级中位数填充（优先级：`cid3+brand_code` → `cid1+brand_code` → 全局中位数）
  - `price` 异常值 → 0.1% / 99.9% 分位数截断
- 保存清洗后 CSV → 转换为 Parquet 格式

### 3.2 前置步骤：Feature_Engineering.ipynb

- **ID 类型统一**：`user_id`、`item_id` → 字符串；`datetime` → datetime 类型
- **异常点击过滤**：按点击量 99.9% 分位数筛除高频点击用户（防爬虫/刷单）
- **RFM + 行为特征**（全局累计）：
  - 订单：购买件数 `F_count`、订单次数、独立商品数、总消费金额、平均客单价、复购率、订单跨度、`R_days`（最近购买距今天数）、平均订单间隔
  - 点击：总点击数、独立商品数、类目/品牌去重数、点击跨度、日均点击、最后点击距今天数
  - 复合特征：`click_to_buy_ratio`、`order_click_ratio`、`active_days`、`has_order`、`has_click`
- **编码**：性别 → 0/1、年龄区间 → OrdinalEncoder、偏态特征 → log1p 变换
- **标准化**：StandardScaler
- **K-Means 聚类**：K=4，生成 `cluster` 标签（0~3）
- 保存 `user_features_raw_with_cluster.parquet`（100,000 行 × 41 列）

### 3.3 主流水线：improved_modeling.py

`improved_modeling.py` 是当前建模的核心脚本，完整流程如下：

#### 步骤 0：数据加载
- 加载 `user_features_raw_with_cluster.parquet`、`user_order_cleaned.parquet`、`user_click.parquet`
- 加载 `item_data_cleaned.parquet`（仅加载 `item_id, cid1, cid2, cid3, brand_code, price` 以节省内存）
- 统一 ID 类型为字符串，datetime 转换

#### 步骤 1-2：时间窗口定义 & 数据划分
- **预测窗口**：数据最后 7 天（2019-02-12 ~ 2019-02-18）
- **观察窗口**：预测窗口前 30 天（2019-01-13 ~ 2019-02-11）
- **标签**：预测窗口内是否有订单 → 正样本约 13%
- **时间顺序划分**：按观察窗口内最后活跃时间排序，前 70% 用户为训练集，后 30% 为测试集

#### 步骤 3：基线特征（30 天观察窗口）
- 观察期内订单聚合：订单次数、购买总件数、独立商品数、活跃天数、订单跨度、平均间隔、距预测起始天数
- 观察期内点击聚合：点击次数、独立商品数、活跃天数、点击跨度、日均点击、距最后点击天数
- 转化特征：`click_to_buy_ratio`、`has_conversion`
- 合并用户基础特征：`gender_encoded`、`age_encoded`

#### 步骤 4：新增特征工程（四类）

**4.1 多时间窗口特征（7/14/30/60 天）**
- 每个窗口：点击/订单次数、独立商品数、活跃天数、距预测起始天数
- 变化率特征：7d/14d、14d/30d、7d/30d 的行为比率（捕捉近期趋势）
- 活跃度衰减：`recency_decay`
- 各窗口的点击→订单转化率

**4.2 类目/品牌转化特征**
- `cid1`（一级类目）：点击去重数、订单去重数、Top1 类目点击数、类目转化率
- `cid3`（三级类目）：点击/订单去重数
- `brand_code`：品牌去重数、Top1 品牌点击数、品牌偏好集中度（`brand_focus_ratio`）
- 交叉特征：`cid1_per_brand`、`top1_cid1_click_ratio`、`order_cid1_concentration`

**4.3 行为路径特征**
- 点击→购买时间差（均值、中位数、最大/最小，单位小时）
- 弃购率：`abandon_rate`（点击但未购买的商品比例）
- 会话深度：`avg_items_per_session`、`max_items_per_session`、`session_count`

**4.4 价格敏感度特征**
- 点击商品价格分位数：P10、P25、P50、P75、P90、均值、标准差、IQR、变异系数
- 购买商品价格：P50、均值、最大值、最小值
- 全局比较：`click_price_vs_global`、`order_price_vs_global`（相对于全局中位数）
- 价格偏好变化：`order_vs_click_price_ratio`（购买 vs 点击价格比）

#### 步骤 5-6：特征合并与数据准备
- 正交合并所有特征块 → 121 维
- 稳健缺失值填充（median → 0 fallback）+ 极端值裁剪（1%/99% 分位数）
- 剔除低方差特征（`VarianceThreshold(0.01)`）→ 98 维
- StandardScaler 标准化

#### 步骤 7：基线模型评估
- LightGBM 默认参数 + 全部特征
- 输出 AUC、AP、Recall/Precision/F1@多阈值、混淆矩阵

#### 步骤 8：特征选择
- **相关性过滤**：移除 |r| > 0.9 的特征（142 对）→ 45 维
- **互信息**（`mutual_info_classif`）：评估每个特征与标签的信息增益
- **LightGBM 特征重要性**（gain）：树模型内置重要性
- 取互信息 Top 50 ∩ LightGBM 重要性 Top 50 → 最终精选特征集

#### 步骤 9：特征选择效果对比（5 折时序交叉验证）
- 实验 A：仅基线特征
- 实验 B：基线 + 全部新特征
- 实验 C：特征选择后

#### 步骤 10：不平衡处理
- 对比 5 种策略：无处理、`class_weight='balanced'`、SMOTE 过采样、随机下采样、SMOTE + 类权重
- 每种策略在测试集上做阈值扫描（0.05~0.95），以 F2-score 为优化目标
- 选出最佳方法与阈值

#### 步骤 11：Optuna 超参数优化
- 搜索空间：`n_estimators`、`learning_rate`、`num_leaves`/`max_depth`、正则化参数等
- 每模型 15 次试验，TPE 采样器，80/20 hold-out 验证
- 对三个模型（LightGBM / XGBoost / CatBoost）分别优化
- 内置异常捕获，失败时回退到默认参数

#### 步骤 12-13：最终模型训练与测试集评估
- 用 Optuna 最优参数 + 类权重训练三个最终模型
- 在严格时间分离的测试集上评估，做阈值优化（F2）

#### 步骤 14：可视化
- 三模型混淆矩阵并列对比
- ROC 曲线（三模型叠加）
- PR 曲线（三模型叠加）
- AUC/Recall 柱状图
- 保存 `model_evaluation_plots.png`

#### 步骤 15：SHAP 可解释性分析
- 对测试集采样 2000 用户做 SHAP 计算（TreeExplainer）
- 输出 Summary Plot（`shap_summary.png`）和 Importance Bar（`shap_importance.png`）
- 打印 Top 10 特征及其对预测的影响方向

#### 步骤 16：业务建议 & 分群分析
- 按预测概率分段（5 档），统计各段实际下单率
- 按 K-Means 聚类标签分组，分析各人群预测效果
- 输出分层运营策略建议

#### 步骤 17：最终汇总 & 模型持久化
- 打印最佳模型名、参数、全量测试指标
- 保存模型、Scaler、特征名至 `data/models/`

---

## 4. 特征工程与模型优化结果

### 4.1 基线复现

| 项目 | 值 |
|------|-----|
| 预测窗口 | 2019-02-12 ~ 2019-02-18 |
| 观察窗口 | 2019-01-13 ~ 2019-02-11 |
| 训练集 | 70,000 用户（正样本率 5.21%） |
| 测试集 | 30,000 用户（正样本率 26.47%） |

**基线 LightGBM（默认参数，14 特征）**：

| 指标 | 值 |
|------|-----|
| AUC | 0.6681 |
| AP | 0.4014 |
| Recall@0.3 | 0.3008 |
| Precision@0.3 | 0.4398 |
| F1@0.3 | 0.3573 |

混淆矩阵 (threshold=0.3)：`[[19076, 3026], [5522, 2376]]`

**主要缺陷**：训练/测试标签分布漂移大（5.2% → 26.5%）、Recall 偏低（0.30）、特征维度低（14 维，缺少类目/趋势/路径信号）。

### 4.2 新增特征列表

| 特征类别 | 特征数 | 代表性特征 | 生成方式 |
|----------|--------|------------|----------|
| 多时间窗口 | 59 | `order_cnt_7d/14d/30d/60d`, `click_cnt_7d/14d/30d/60d` | 7/14/30/60 天滑动窗口聚合 |
| 行为变化率（趋势） | 25 | `order_cnt_7d_14d_ratio`, `conversion_rate_30d`, `recency_decay` | 窗口间行为比率、衰减率 |
| 类目/品牌转化 | 18 | `cid1_conversion_rate`, `brand_focus_ratio`, `top1_cid1_click_ratio` | 类目级点击-订单转化率、品牌偏好集中度 |
| 行为路径 | 8 | `avg_click_to_order_hours`, `abandon_rate`, `avg_items_per_session` | 点击到购买时间差、弃购率、会话深度 |
| 价格敏感度 | 13 | `click_price_p10/p50/p90`, `order_vs_click_price_ratio`, `click_price_vs_global` | 价格分位数、全局比价、升级/降级比 |

总计：14 维基线 → **119 维** → 特征选择后 **45 维**。

### 4.3 特征选择与验证

**选择流程**：相关性过滤（|r|>0.9，142 对移除）→ 互信息 Top 50 → LightGBM 重要性 Top 50 → 交集保留。

**5 折时序交叉验证对比**：

| 实验 | 特征数 | AUC (CV mean ± std) | Recall@0.3 (CV mean ± std) |
|------|--------|----------------------|----------------------------|
| A: 仅基线 | 14 | 0.7684 ± 0.0164 | 0.1032 ± 0.0078 |
| B: 基线 + 新特征 | 45 | 0.7679 ± 0.0213 | 0.0879 ± 0.0065 |
| C: 特征选择后 | 45 | 0.7679 ± 0.0213 | 0.0879 ± 0.0065 |

> 交叉验证 AUC ~0.77 反映同时间段内的排序能力。测试集（未来时间段）因分布漂移，AUC 降至 ~0.69。

### 4.4 不平衡处理效果

| 方法 | AUC | Recall@0.3 | Precision@0.3 | F1@0.3 |
|------|-----|------------|---------------|--------|
| 无处理 | 0.6633 | 0.3468 | 0.4479 | 0.3909 |
| **类权重** | 0.6252 | **0.8177** | 0.2944 | **0.4329** |
| SMOTE | 0.6589 | 0.2764 | 0.4631 | 0.3462 |
| 下采样 | 0.6371 | 0.9906 | 0.2669 | 0.4205 |
| SMOTE + 权重 | 0.6589 | 0.2764 | 0.4631 | 0.3462 |

**结论**：`class_weight='balanced'` 在 F2-score 优化下召回率提升最显著（0.35 → 0.82），被选为所有后续模型的默认策略。

### 4.5 超参数优化结果

Optuna（TPE 采样器），每模型 15 次试验，80/20 hold-out 验证：

| 模型 | 验证 AUC | 最优参数 |
|------|----------|----------|
| LightGBM | 0.7916 | `n_estimators=186, lr=0.0196, num_leaves=44, max_depth=4, reg_alpha=0.138` |
| XGBoost | **0.7928** | `n_estimators=132, lr=0.0214, max_depth=3, reg_alpha=0.948, colsample=0.858` |
| CatBoost | 0.7922 | `iterations=201, lr=0.0225, depth=6, l2_leaf_reg=0.409` |

### 4.6 最终测试集评估

| 模型 | AUC | AP | 最佳阈值 | Recall | Precision | F1 | F2 |
|------|-----|----|----------|--------|-----------|-----|-----|
| LightGBM | 0.6917 | 0.4308 | 0.600 | 0.9582 | 0.2838 | 0.4379 | 0.6495 |
| **XGBoost** | **0.6959** | 0.4342 | 0.625 | 0.9490 | 0.2890 | **0.4431** | **0.6514** |
| CatBoost | 0.6849 | **0.4394** | 0.575 | 0.9528 | 0.2852 | 0.4390 | 0.6489 |

**XGBoost 最优模型混淆矩阵 (threshold=0.625)**：

| 真实\预测 | 未下单 | 下单 |
|-----------|--------|------|
| 未下单 | 3,491 | 18,607 |
| 下单 | 403 | 7,495 |

**指标变化总结**：

| 指标 | 基线 (LightGBM) | 最优模型 (XGBoost) | 提升 |
|------|----------------|--------------------|------|
| AUC | 0.6681 | **0.6959** | +4.2% |
| AP | 0.4014 | **0.4342** | +8.2% |
| Recall | 0.3008 | **0.9490** | +215% |
| F1 | 0.3573 | **0.4431** | +24.0% |

> AUC 提升受限于训练/测试集时间分布漂移（5.2% → 26.5% 正样本率）。交叉验证 AUC 从 0.77 → 0.79，外推能力的实际改善体现在 AP 和 F1 上。

### 4.7 SHAP 可解释性分析

**Top 10 最重要特征**：

| 排名 | 特征名 | SHAP 重要性 | 方向 | 业务含义 |
|------|--------|-------------|------|----------|
| 1 | `brand_focus_ratio` | 0.276 | ↑ 促进 | 品牌偏好越集中，购买意愿越强 |
| 2 | `cid1_click_nunique` | 0.180 | ↑ 促进 | 浏览品类越多，购买可能性越高 |
| 3 | `click_span` | 0.161 | ↑ 促进 | 点击行为持续时间长，用户粘性好 |
| 4 | `obs_days_since_last` | 0.139 | ↑ 促进 | 最近有下单行为更可能再次购买 |
| 5 | `obs_avg_interval` | 0.120 | ↑ 促进 | 下单间隔规律的用户购买习惯稳定 |
| 6 | `click_cnt` | 0.115 | ↑ 促进 | 点击量大的用户活跃度高 |
| 7 | `cid1_per_brand` | 0.086 | ↑ 促进 | 品类/品牌多样性体现探索意愿 |
| 8 | `obs_active_days` | 0.070 | ↑ 促进 | 观察期活跃天数越多越可能购买 |
| 9 | `brand_click_top1_count` | 0.065 | ↑ 促进 | 对偏好品牌的高频点击预示购买 |
| 10 | `days_since_last_click` | 0.063 | ↑ 促进 | 最近点击过的用户转化概率高 |

### 4.8 业务建议

#### 按预测概率分层运营

| 概率区间 | 用户数 | 实际下单率 | 运营策略 |
|----------|--------|------------|----------|
| 很高 (0.8-1.0) | 10,072 | **42.7%** | 大额优惠券 + 新品推送 |
| 高 (0.6-0.8) | 16,799 | 19.8% | 限时折扣提醒 + "浏览未购"召回 |
| 中 (0.4-0.6) | 3,107 | 9.0% | 品类促销 + 低门槛券激活 |
| 低 (0.2-0.4) | 22 | 13.6% | 大促/季节性批量召回 |

#### 分群运营建议

- **Cluster 1（高价值用户）**：推送高单价新品 + 会员专属折扣，利用 `brand_focus_ratio` 定向偏好品牌
- **Cluster 2（中坚用户）**：推送热销品类，强化品类浏览→购买的转化路径，降低 `abandon_rate`
- **Cluster 3（沉睡用户）**：大促活动召回，关注 `days_since_last_click` 短的"近期回访"用户

#### 部署建议

- **推荐阈值**：0.625（F2-score 优化），平衡召回率（95%）与精确率（29%）
- 若追求高转化率（Precision > 0.5）：阈值提升至 0.85，召回率约 40%，实际下单率 50%+
- 模型文件：`data/models/best_purchase_model.pkl`

### 4.9 改进总结

| 改进项 | 方法 | 效果 |
|--------|------|------|
| 多时间窗口特征 | 7/14/30/60 天滑动窗口 + 变化率 | 捕捉短期活跃度变化 |
| 类目/品牌特征 | cid1/cid3 转化率 + 品牌偏好集中度 | SHAP Top 1 特征（`brand_focus_ratio`） |
| 行为路径特征 | 点击→购买时间差 + 弃购率 + 会话深度 | 刻画购买决策效率 |
| 价格敏感度 | 价格分位数 + 全局比价 + 升级/降级比 | 反映用户价格段偏好 |
| 特征选择 | 相关性过滤 + 互信息 + LGB 重要性 | 119 → 45 维精选 |
| 类权重 | `class_weight='balanced'` | 召回率 +215% |
| 超参数优化 | Optuna TPE × 3 模型 | 验证 AUC 达 0.79 |
| SHAP 可解释性 | TreeExplainer | 特征方向明确，指导运营 |

**核心发现**：品牌偏好集中度（`brand_focus_ratio`）是预测购买的最强信号，用户浏览品类广度（`cid1_click_nunique`）和近因行为（`obs_days_since_last`, `days_since_last_click`）同样关键。这些新增特征使模型 AUC 从 **0.668 → 0.696**，AP 从 **0.401 → 0.434**，在极具挑战性的时序外推场景下实现了可量化的提升。

---

## 5. 前端可视化系统

### 5.1 架构设计

采用 **前后端分离** 架构：FastAPI 提供 RESTful 数据 API + 模型推理服务，前端为独立 HTML/CSS/JS 单页应用（内嵌 Plotly.js），通过 API 实时获取数据并渲染交互式图表。

```
┌─────────────────────────┐        REST API        ┌──────────────────────────┐
│   Dashboard (SPA)       │ ◄───────────────────► │   FastAPI Backend        │
│   · Plotly.js 图表       │    JSON / HTTP         │   · 预计算缓存 (< 2s 启动) │
│   · 5 个功能模块          │                        │   · 模型推理 (XGBoost)     │
│   · 侧栏导航 + 用户搜索   │    Port 8000           │   · NaN 安全清洗          │
│   · 浅色画廊主题          │                        │   · 25 个 API 端点        │
└─────────────────────────┘                        └──────────────────────────┘
```

| 层 | 技术 | 说明 |
|----|------|------|
| 后端框架 | FastAPI + Uvicorn | 异步支持、自动 OpenAPI 文档 |
| 数据缓存 | JSON + Parquet | 预计算所有聚合指标，避免实时查询大数据集 |
| 前端渲染 | 原生 HTML/CSS/JS | 无框架依赖，单文件 47KB |
| 图表引擎 | Plotly.js 2.32 | CDN 加载，支持交互、缩放、导出 |
| 模型服务 | XGBoost + joblib | 100K 用户预测结果预存为 Parquet 查找表 |
| 样式系统 | CSS Variables | 全局设计令牌，暖白画廊主题 |

### 5.2 数据预计算

为规避原始数据量过大（点击 52M 行、商品 6.8M 行）导致的启动延迟，设计了离线预计算管线 `prepare_dashboard_data.py`：

**特征重建**：完整复现 `improved_modeling.py` 的 119 维特征工程管线（多时间窗口、类目/品牌转化、行为路径、价格敏感度），经 StandardScaler → VarianceThreshold → 相关性过滤，对齐模型训练时的 45 维特征集，对全部 100,000 用户生成预测概率。

**缓存产物**（`data/dashboard_cache/`）：

| 文件 | 大小 | 内容 |
|------|------|------|
| `overview.json` | 2.8 KB | KPI、性别/年龄/RFM 分布、24 小时活跃度 |
| `clusters.json` | 7.2 KB | 四群组画像、群组×品类交叉统计 |
| `propensity.json` | 2.6 KB | 预测概率分布、倾向分层、SHAP Top 15 |
| `behavior_path.json` | 1.7 KB | 点击→购买时间分布、品类漏斗 |
| `products.json` | 9.4 KB | Top 30 商品/品类/品牌/价位分布 |
| `user_predictions.parquet` | 1.1 MB | 100K 用户预测概率 + 标签 |

后端启动时加载全部缓存仅需约 2 秒（对比直接加载 Parquet 原始数据需 60+ 秒）。

### 5.3 功能模块

#### 模块 1：用户画像总览

| 图表 | 类型 | 说明 |
|------|------|------|
| KPI 卡片 | 4 列网格 | 总用户 100K、有订单 66K、有浏览 85K、转化率 66.4% |
| 性别分布 | 环形饼图 | 男女比例，中心显示总用户数 |
| 年龄分布 | 柱状图 | 各年龄区间用户数量 |
| Recency 分布 | 柱状图 | 6 段：0-7天 ~ 180-365天 |
| Frequency 分布 | 柱状图 | 6 段：0 ~ 100+ 次 |
| 时段活跃度 | 双轴折线 | 左轴=订单量 (~73K)，右轴=点击量 (~2.6M)，量级差 35× |

#### 模块 2：购买倾向分析

| 图表 | 类型 | 说明 |
|------|------|------|
| 预测概率分布 | 柱状图 | 10 段概率区间，标注最佳阈值 0.625 |
| 购买倾向分层 | 环形图 | 高/中/低三档占比 |
| SHAP 特征重要性 | 横向柱状图 | Top 15 特征，标注促进/抑制方向 |
| 批量预测查询 | 表单+表格 | 输入 User ID 列表，返回概率+倾向+预测标签 |

#### 模块 3：行为路径追踪

| 图表 | 类型 | 说明 |
|------|------|------|
| 点击→购买转化时间 | 柱状+折线 | <1小时 ~ 7-30天 分段 |
| 品类点击–购买漏斗 | 双轴横向柱 | 左轴=点击量，右轴=订单量，量级差 70× |
| 单用户行为路径查询 | 统计卡片 | 浏览/购买/转化率/最近活跃 |

#### 模块 4：用户分群与标签管理

| 图表 | 类型 | 说明 |
|------|------|------|
| 群组 KPI 卡片 | 4 列网格 | 中坚/高价值/流失/极低价值用户 |
| 分群画像雷达图 | 极坐标 | R/F/M/点击/订单率五维归一化 |
| 群组特征对比表 | 数据表 | 各群组的核心指标横向对比 |
| 群组×品类热力图 | 热力图 | Top 12 品类在各群组的订单占比 |

#### 模块 5：热销商品分析

| 图表 | 类型 | 说明 |
|------|------|------|
| 品类销售分布 | Treemap | 一级品类订单量占比 |
| 购买商品价位分布 | 柱状图 | 8 段价位区间订单量 |
| 价格×销量关系 | 气泡散点图 | x=单价，y=订单数，大小=销量，颜色=品类 |
| 热销品类均价对比 | 横向柱状图 | 各品类 Top 商品均价 |
| 商品明细表 | 数据表 | Top 30 商品全字段，含销售额 |

### 5.4 设计系统

采用 **"Warm Gallery"（暖白画廊）** 美学方向，受艺术画廊空间启发：

| 要素 | 规格 |
|------|------|
| 底色 | `#faf8f5` 暖白（模拟画廊墙面） |
| 卡片 | 纯白 `#ffffff` + 微阴影 |
| 强调色 | 哑光金 `#a8893c`（关键数据）、钢蓝 `#5a7a8a`（二级信息） |
| 告警色 | 暖赤 `#c1665a`（高倾向标签） |
| 展示字体 | Playfair Display（标题）、JetBrains Mono（数据等宽） |
| 正文字体 | Noto Sans SC（中文正文） |
| 质感 | 极淡纸纹噪点叠加层 (opacity: 0.018) |
| 动效 | 交错入场序列 (stagger 0.04s × 6)、悬停下划线展开、卡片微抬 |
| 图表配色 | 7 色循环调色板，与整体暖色系协调 |

### 5.5 关键问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 后端启动极慢 | 直接加载 2.7GB Parquet 文件 | 预计算 JSON 缓存，启动 < 2s |
| 时段图表订单/点击不可共轴 | 量级差 35× | 双 y 轴：左轴=订单，右轴=点击 |
| 品类漏斗点击/订单不可共轴 | 量级差 70× | 双 x 轴：下轴=点击，上轴=订单 |
| 柱状图标签被裁切 | 底部边距不足 | `margin.b` 提至 70-100px，`automargin: true` |
| 坐标轴配置丢失 | JS 浅合并替换整对象 | 实现 `baseLayout()` 深度合并 |
| 部分图表完全空白 | `const` 变量被重新赋值 | 改为 `let` 解构 |
| 热销商品 API 返回 500 | 已下架商品含 NaN 字段 | 递归清洗 `NaN/Inf → null` |
| 热销明细表显示裸 ID | `item_data` 无匹配记录 | 前端过滤 `item_name` 为 null 的记录 |

### 5.6 启动方式

```bash
# 1. 数据预计算（首次或数据更新后执行）
python3 prepare_dashboard_data.py

# 2. 启动后端
uvicorn backend_api:app --host 0.0.0.0 --port 8000

# 3. 浏览器访问
open http://localhost:8000
```

### 5.7 新增文件

| 文件 | 职责 |
|------|------|
| `backend_api.py` | FastAPI 后端服务，25 个 API 端点，含 NaN 安全清洗与静态文件服务 |
| `dashboard.py` | Streamlit 备用仪表盘（功能完整，样式受限于 Streamlit 框架） |
| `static/dashboard.html` | **主要仪表盘** — 独立 SPA，暖白画廊主题，内嵌 Plotly.js |
| `prepare_dashboard_data.py` | 离线数据预计算脚本，含完整 119 维特征管线 + 全量用户预测 |
| `requirements_viz.txt` | 可视化系统 Python 依赖 |

---

## 6. 模型局限性分析与前沿演进展望
本章针对固定的 IJCAI 19 数据集环境及单机算力约束，深入剖析系统在学术研究与实际业务应用中的瓶颈，并结合工业界主流大数据与算法架构，提出前沿的演进技术方案。这不仅为本毕业设计提供了严谨的错误案例分析，也为后续的系统升级指明了方向。

---

### 6.1 核心局限性与错误案例分析 (Bad Case Analysis)

1. **时序非平稳性导致的外推衰减（Data Drift）**

   * **现象**：测试集包含大促节点，正样本率（26.47%）远高于训练集（5.21%）。
   * **成因**：训练于平销期数据，无法捕捉促销、节日等外部因素导致的爆发式购买意愿，跨时间窗口预测时 AUC 衰减（0.79 → 0.69）。
   * **改进措施**：

     * 引入时间加权特征、节假日标记和外部事件特征，缓解分布漂移，提高跨时间段预测准确率。

2. **低频冷启动用户特征退化**

   * **现象**：观察期点击 <5 次的用户组 AUC≈0.531，接近随机猜测。
   * **成因**：高级特征依赖历史行为密集数据，低频用户特征稀疏或退化为 0。
   * **改进措施**：

     * 增加人口统计、注册天数、设备类型等通用特征；
     * 使用协同过滤或 Embedding 特征提升低频用户预测能力。

3. **“深度闲逛比价型”用户误判**

   * **现象**：F2-score 优化后 Recall≈94.9%，Precision≈28.9%，假阳性高。
   * **成因**：高频点击、跨类目浏览的用户行为与高购买意向用户相似，但实际不购买。
   * **改进措施**：

     * 使用多任务模型或 Uplift Model 区分“只浏览用户”与“购买意向用户”，提高 Precision。
     * 可通过因果推断预测干预增量（CATE），精准营销。

4. **特征工程复杂度高**

   * **现象**：初始 119 维特征，最终精选 45 维仍依赖历史行为。
   * **影响**：增加模型训练和部署复杂度，对低频用户效果有限。
   * **改进措施**：

     * 引入自动特征选择、Embedding 特征或深度序列模型（DIN/Transformer），降低手工复杂度，提高泛化能力。

5. **前端数据延迟**

   * **现象**：离线预计算数据无法实时反映秒级用户行为变化。
   * **影响**：实时推荐或操作响应受限。
   * **改进措施**：

     * 搭建流式特征存储 + 在线预测（Kafka + Flink + Redis）；
     * 实现秒级响应，实现千人千面推荐。

---

### 6.2 工业级架构与算法演进展望 (Future Work)

若脱离单机环境与固定数据集的约束，系统可演进为以下前沿架构：

1. **算法模型：从“倾向预测”升级为“因果推断 (Uplift Model)”**

   * 现有模型仅预测是否购买，无法区分自然转化者与营销敏感者。
   * **方案**：引入 A/B Test 数据，构建基于增量效果的 Uplift Model。
   * **业务价值**：精准聚焦营销预算于犹豫用户，提高 ROI。

2. **特征工程：从手工统计特征到深度行为序列学习 (DIN/Transformer)**

   * 当前特征依赖滑动窗口统计，无法捕捉用户点击间的时间序列关系。
   * **方案**：使用 DIN 或 Transformer，动态激活相关历史点击，提高意图识别精度。

3. **数据架构：从静态缓存到流式特征存储 (Feature Store)**

   * 离线 JSON 预计算无法实时捕捉用户行为。
   * **方案**：结合 Hive 离线特征 + Kafka/Flink 流式特征 + Redis 在线拼接，实现 Near-line Inference 秒级响应。


```
┌────────────────────────┐
│  历史离线数据 (Hive)   ├─► 离线特征提取 (T+1) ──┐
└────────────────────────┘                         │
                                                   ▼
┌────────────────────────┐                  ┌─────────────┐     实时拼接      ┌──────────────┐
│ 用户实时点击流 (Kafka) ├─► 流式计算 (Flink) ─►┤ Redis (Online│ ───────────► │  FastAPI 在线 │
└────────────────────────┘   (近 5 分钟特征)    │ Feature Store)│              │   模型推理   │
                                            └─────────────┘                    └──────────────┘
```

---

### 6.3 总结

* 系统核心功能已完成，画像分群与购买倾向预测目标达成。
* 现存问题包括：

  * 时序漂移导致外推能力下降；
  * 冷启动和低频用户预测效果不佳；
  * 高假阳性率影响精准度；
  * 手工特征工程复杂；
  * 前端无法实时响应。
* 改进措施：

  1. 增强特征对时序漂移适应能力；
  2. 提升冷启动与低频用户预测准确性；
  3. 减少假阳性率，提高 Precision；
  4. 引入深度行为序列模型优化特征；
  5. 构建实时流式特征与在线预测；
  6. 定期模型更新与性能监控。

通过这些改进，系统可进一步提高预测准确率和业务价值，为新客激活与老客召回提供更可靠支持。

---